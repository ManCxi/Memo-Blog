<% const activePage='dashboard' %>
  <%- include('partials/layout-head') %>

    <!-- Stat cards -->
    <div class="stat-grid">
      <div class="stat-card">
        <div class="stat-icon stat-icon-blue">📝</div>
        <div>
          <div class="stat-value">
            <%= stats.articleCount %>
          </div>
          <div class="stat-label">全部文章</div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon stat-icon-green">✅</div>
        <div>
          <div class="stat-value">
            <%= stats.publishedCount %>
          </div>
          <div class="stat-label">已发布</div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon stat-icon-yellow">🗂️</div>
        <div>
          <div class="stat-value">
            <%= stats.categoryCount %>
          </div>
          <div class="stat-label">分类数量</div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon stat-icon-orange">🏷️</div>
        <div>
          <div class="stat-value">
            <%= stats.tagCount %>
          </div>
          <div class="stat-label">标签数量</div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon" style="background:var(--accent-tint);">👁️</div>
        <div>
          <div class="stat-value">
            <%= stats.totalPv ? stats.totalPv.toLocaleString() : 0 %>
          </div>
          <div class="stat-label">总阅读量</div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon stat-icon-purple">📄</div>
        <div>
          <div class="stat-value">
            <%= stats.pageCount %>
          </div>
          <div class="stat-label">独立页面</div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon" style="background:var(--admin-yellow-tint);color:var(--admin-yellow);">📦</div>
        <div>
          <div class="stat-value">
            <%= stats.draftCount %>
          </div>
          <div class="stat-label">草稿箱</div>
        </div>
      </div>
    </div>

    <!-- Quick actions -->
    <div class="quick-actions mb-20">
      <a href="/admin/articles/create" class="btn btn-primary">✏️ 写新文章</a>
      <a href="/admin/pages" class="btn btn-secondary">📄 页面管理</a>
      <a href="/admin/articles" class="btn btn-secondary">📋 管理文章</a>
      <a href="/admin/taxonomy" class="btn btn-secondary">🗂️ 分类/标签管理</a>
      <a href="/admin/media" class="btn btn-secondary">🖼️ 附件库</a>
      <a href="/admin/settings" class="btn btn-secondary">⚙️ 系统设置</a>
      <a href="/" target="_blank" class="btn btn-secondary">🌐 返回前台</a>
    </div>

    <!-- Recent Published articles -->
    <div class="table-card mb-20">
      <div class="table-card-header">
        <span class="table-card-title">🚀 最近发布的文章</span>
        <a href="/admin/articles?status=published" class="btn btn-secondary btn-sm">查看全部</a>
      </div>

      <div class="table-responsive-wrap">
        <div style="overflow-x:auto;">
          <table class="data-table">
            <thead>
              <tr>
                <th style="width:38%">标题</th>
                <th>分类</th>
                <th>阅读量</th>
                <th>发布时间</th>
                <th class="actions-col">操作</th>
              </tr>
            </thead>
            <tbody>
              <% if (recentPublished.length===0) { %>
                <tr>
                  <td colspan="5" class="table-empty">暂无已发布文章</td>
                </tr>
                <% } %>
                  <% recentPublished.forEach(article=> { %>
                    <tr>
                      <td>
                        <a href="/article/<%= article.slug %>" target="_blank"
                          style="font-weight:600;color:var(--text-primary);text-decoration:none;transition:color 0.15s;"
                          onmouseover="this.style.color='var(--accent)'"
                          onmouseout="this.style.color='var(--text-primary)'">
                          <%= article.title %>
                        </a>
                      </td>
                      <td>
                        <% if (article.category) { %>
                          <span class="status-badge badge-category"><%= article.category.name %></span>
                        <% } else { %>
                          <span style="color:var(--text-tertiary);">—</span>
                        <% } %>
                      </td>
                      <td class="text-tertiary text-sm">👁 <%= (article.views || 0).toLocaleString() %></td>
                      <td class="text-tertiary text-sm">
                        <%= new Date(article.publishedAt).toLocaleString('zh-CN', { year:'numeric', month:'2-digit', day:'2-digit', hour:'2-digit', minute:'2-digit' }) %>
                      </td>
                      <td>
                        <a href="/admin/articles/<%= article.id %>/edit" class="btn btn-edit btn-sm">✏️ 编辑</a>
                      </td>
                    </tr>
                    <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <!-- Recent Drafts -->
    <div class="table-card">
      <div class="table-card-header">
        <span class="table-card-title">📄 我的草稿箱</span>
        <a href="/admin/articles?status=draft" class="btn btn-secondary btn-sm">查看全部</a>
      </div>

      <div class="table-responsive-wrap">
        <div style="overflow-x:auto;">
          <table class="data-table">
            <thead>
              <tr>
                <th style="width:38%">标题</th>
                <th>分类</th>
                <th>上次修改</th>
                <th class="actions-col">操作</th>
              </tr>
            </thead>
            <tbody>
              <% if (recentDrafts.length===0) { %>
                <tr>
                  <td colspan="4" class="table-empty">草稿箱空空如也，<a href="/admin/articles/create" style="color:var(--accent);">写点什么吧</a></td>
                </tr>
                <% } %>
                  <% recentDrafts.forEach(article=> { %>
                    <tr>
                      <td>
                        <a href="/admin/articles/<%= article.id %>/preview" target="_blank"
                          style="font-weight:600;color:var(--text-primary);text-decoration:none;transition:color 0.15s;"
                          onmouseover="this.style.color='var(--accent)'"
                          onmouseout="this.style.color='var(--text-primary)'">
                          <%= article.title %>
                        </a>
                      </td>
                      <td>
                        <% if (article.category) { %>
                          <span class="status-badge badge-category"><%= article.category.name %></span>
                        <% } else { %>
                          <span style="color:var(--text-tertiary);">—</span>
                        <% } %>
                      </td>
                      <td class="text-tertiary text-sm">
                        <%= new Date(article.updatedAt || article.createdAt).toLocaleString('zh-CN', { year:'numeric', month:'2-digit', day:'2-digit', hour:'2-digit', minute:'2-digit' }) %>
                      </td>
                      <td>
                        <a href="/admin/articles/<%= article.id %>/edit" class="btn btn-edit btn-sm">✏️ 编辑</a>
                      </td>
                    </tr>
                    <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <%- include('partials/layout-foot') %>